

<html>
<head>
  <title>User Profile Form</title>
  <link rel="stylesheet" href="../styles/profile.css">
</head>
<body>
  <div class="container">
    <h1>User Profile Form</h1>
    <form action="/userprofile" method="post">
      <label for="name">Name:</label>
      <input type="text" name="name" required value="<%= profile.length > 0 && profile[0].profileDetails.length > 0 ? profile[0].profileDetails[0].name : '' %>"><br>

      <label for="address">Address:</label>
      <input type="text" name="address" required value="<%= profile.length > 0 && profile[0].profileDetails.length > 0 ? profile[0].profileDetails[0].address : '' %>"><br>

      <label for="place">Place:</label>
      <input type="text" name="place" required value="<%= profile.length > 0 && profile[0].profileDetails.length > 0 ? profile[0].profileDetails[0].place : '' %>"><br>

      <label for="phone">Phone:</label>
      <input type="text" name="phone" required value="<%= profile.length > 0 && profile[0].profileDetails.length > 0 ? profile[0].profileDetails[0].phone : '' %>"><br>

      <button type="submit">Submit</button>
    </form>
  </div>
</body>
</html>
